/**
 * Path to the app info store file
 */
export const APP_INFO_STORE_PATH = 'app_info.json';

/**
 * Path to the presets store file
 */
export const PRESETS_STORE_PATH = 'presets.json';

/**
 * Path to the tasks store file
 */
export const TASKS_STORE_PATH = 'tasks.json';

/**
 * Path to the configuration store file
 */
export const CONFIG_STORE_PATH = 'config.json';

/**
 * Path to the files store file
 */
export const FILES_STORE_PATH = 'files.json';

/**
 * Keys used in the presets store
 */
export const PRESETS_STORE_KEYS = {
  PRESETS: 'presets',
};

/**
 * Keys used in the tasks store
 */
export const TASKS_STORE_KEYS = {
  TASKS: 'tasks',
  QUEUE: 'queue',
};

/**
 * Keys used in the configuration store
 */
export const CONFIG_STORE_KEYS = {
  CONFIG: 'config',
};

/**
 * Keys used in the files store
 */
export const FILES_STORE_KEYS = {
  FILES: 'files',
  SELECTED_FILE: 'selectedFile',
};

/**
 * Keys used in the app info store
 */
export const APP_INFO_STORE_KEYS = {
  APP_INFO: 'app_info',
};
